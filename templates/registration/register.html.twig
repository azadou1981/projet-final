{% extends 'base.html.twig' %}

{% block title %}Inscription - CRYSTAL{% endblock %}

{% block body %}
<div class="form-card">
    <h1 style="margin-top: 0;">Inscription</h1>

    {{ form_errors(registrationForm) }}

    {{ form_start(registrationForm) }}
        <div class="form-group">
            {{ form_label(registrationForm.email, 'Email') }}
            {{ form_widget(registrationForm.email, {'attr': {'placeholder': 'exemple@email.com'}}) }}
        </div>

        <div class="form-group">
            {{ form_label(registrationForm.plainPassword, 'Mot de passe') }}
            {{ form_widget(registrationForm.plainPassword, {'attr': {'placeholder': '••••••••'}}) }}
        </div>

        <div class="form-group checkbox-group">
            {{ form_widget(registrationForm.agreeTerms) }}
            {{ form_label(registrationForm.agreeTerms) }}
        </div>

        <button type="submit" class="btn">S'inscrire →</button>
    {{ form_end(registrationForm) }}

    <p style="text-align: center; margin-top: 1.5rem; color: var(--gris);">
        Déjà inscrit ? <a href="{{ path('app_login') }}" style="color: var(--or); font-weight: 600;">Se connecter</a>
    </p>
</div>
{% endblock %}
